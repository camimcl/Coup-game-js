export default class Match {
  constructor() {
    // Precisamos de um estado, ou seja, como a partida esta desenrolando (class GameState)
    // Precisamos dos jogadores participantes (class Player)
    // code
    // host: Player
    // MIN_PLAYERS: 3
    // MAX_PLAYERS: 10
    // has_ended: bool
    // winner: Player
  }
}
